node_modules/
.next/
.env
.env.local
.env.production
npm-debug.log*
*.tsbuildinfo

# Runtime product image uploads (local filesystem storage)

# Runtime data kept outside the `next dev` watcher (product image uploads
# in local development via PRODUCT_UPLOAD_DIR)
